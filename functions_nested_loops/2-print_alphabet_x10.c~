#include "main.h"

/** 
 *functions_nested_loops - first nested loop
*/

void print_alphabet_void(void)
{
  int n = 0;
  while (n < 10)
  {
    m = 'a';
    while (m <= 'z')
    {
      _pushchar(m);
      m++;
    }
    _pushchar('\n');
    n++;
  }
}
