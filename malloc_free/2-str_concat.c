#include "main.h"
#include <stdio.h>


char *str_concat(char *s1, char *s2)
{
  char *sum;
  if (s1 == NULL)
    {
      return (s2);
    }
  if (s2 == NULL)
    {
      return (s1);
    }
  
  if (sum == NULL)
    {
      return (NULL);
    }
  return (sum);
}
