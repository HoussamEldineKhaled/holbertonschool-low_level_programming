#include "main.h"
/**
 * put_half - half of str
 * @str: input str
*/

void puts_half(char *str)
{
int len = 0, i;
while (str[len] != '\0')
{
len++;
}
i = (len - 1) / 2;
while (i < len)
{
_putchar(str[i]);
i++;
}
_putchar('\n');
}
