#include "main.h"
/**
 * rev_string - reverse string
 * @s: given string
*/
void rev_string(char *s)
{
int len = 0;
int i;
int j;
char *m = "";
while (s[len] != '\0')
{
len++;
}
i = len - 1;
j = 0;
while (i >= 0)
{
m[j] = s[i];
i--;
j++;
}
*s = *m;
}
